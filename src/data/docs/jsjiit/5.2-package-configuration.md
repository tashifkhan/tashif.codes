# Package configuration

`package.json` for jsjiit **0.0.22**. Build behavior: [Build system](5.1-build-system). Dev deps: [Dependency management](5.3-dependency-management).

## Identity

| Field | Value |
| --- | --- |
| `name` | `jsjiit` |
| `version` | `0.0.22` |
| `license` | `ISC` |
| `author` | `codeblech` |
| `description` | Browser-compatible API for JIIT WebPortal. Bypasses CAPTCHA. |

`repository` / `bugs` / `homepage` still point at `https://github.com/codeblech/jsjiit`. This fork lives at [tashifkhan/jsjiit](https://github.com/tashifkhan/jsjiit).

## Entry resolution

```mermaid
flowchart TD
  spec[import 'jsjiit'] --> exports[exports.import]
  exports --> file[dist/jsjiit.esm.js]
  legacy[main] --> src[src/index.js]
  moduleF[module / browser] --> file
```

```json
"type": "module",
"main": "src/index.js",
"module": "dist/jsjiit.esm.js",
"browser": "dist/jsjiit.esm.js",
"exports": {
  "import": "./dist/jsjiit.esm.js",
  "require": "./dist/jsjiit.esm.js"
}
```

`require` still loads ESM. A CJS `require("jsjiit")` will not work in Node.

## Files on npm

```mermaid
flowchart LR
  include["files: dist, src"] --> pack[npm pack]
  ignore[.npmignore] --> omit
```

`.npmignore`:

```
cert.pem
key.pem
.vscode
docs
node_modules
run_server
index.html
```

```mermaid
flowchart TB
  keep[dist + src + package.json]
  drop[run_server test.html docs certs]
```

`test.html` is not in `.npmignore` and not in `files`. npm `files` whitelist wins, so `test.html` stays out of the tarball.

## Scripts

```mermaid
flowchart LR
  test["test: echo and exit 1"]
  docs["docs: jsdoc -c jsdoc.conf.json --verbose"]
  build["build: node build.mjs"]
  prepare["prepare: npm run build"]
```

There is no real test runner. `"test"` always fails.

## Keywords

The keyword list is attendance-heavy (`jaypee-attendance-api`, …). Useful for npm search, not for the API.

## Lockfile

```mermaid
flowchart LR
  pkg[package.json 0.0.22] --> lock[package-lock.json lockfileVersion 3]
```

`lockfileVersion` 3. Dev tree includes esbuild and jsdoc (and jsdoc's Babel parser).

## Dual consume

```mermaid
flowchart TB
  npmInstall[npm install jsjiit@0.0.22] --> bundler[import from jsjiit]
  cdn[jsDelivr] --> script[script type=module]
```

```mermaid
flowchart LR
  v[0.0.22 this clone] --> npmCheck[npm show jsjiit version]
  npmCheck -->|newer| warn[read that source, do not invent APIs]
```

## License field only

ISC in `package.json`. There is no separate `LICENSE` file in the clone listing.

```mermaid
flowchart TB
  scripts[scripts]
  scripts --> test[always fails]
  scripts --> docs[jsdoc]
  scripts --> build[esbuild]
  scripts --> prepare[build on publish]
```

```mermaid
flowchart LR
  exports[exports.import] --> esm[jsjiit.esm.js]
  cdnTip[these docs] --> min[jsjiit.min.esm.js]
```
