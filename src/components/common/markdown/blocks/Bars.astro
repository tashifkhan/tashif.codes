---
import type { BlockProps } from "./shared";
const { attrs, theme } = Astro.props as BlockProps;
---
<div class:list={["md-bars", theme.bars]}>
  {attrs.title && <p class="md-bars-title">{attrs.title}</p>}
  <slot />
</div>
