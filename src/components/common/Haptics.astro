---
// Shared by the base layout and the standalone docs layout.
// Defaults cover links and controls. data-haptic can override the semantic
// pattern; "manual" delegates to an action handler, "off" silences a subtree.
---
<script>
	import { installHaptics } from "@/lib/haptics";
	installHaptics();
</script>
