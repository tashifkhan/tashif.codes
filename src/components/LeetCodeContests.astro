---
import { leetCodeData } from "../data/leetcode";
import { Trophy } from "lucide-react";
import EmptyState from "./EmptyState.astro";

const { contests } = leetCodeData;
---

<section
	class="rounded-xl border border-border/60 bg-card/60 backdrop-blur-sm overflow-hidden h-fit"
>
	<div class="px-5 py-3.5 border-b border-border/60 flex items-center gap-2">
		<Trophy className="w-3.5 h-3.5 text-muted-foreground" />
		<span
			class="text-[10px] font-bold uppercase tracking-[0.18em] text-muted-foreground"
		>
			Contest Performance
		</span>
	</div>
	{
		contests.length > 0 ? (
			<div class="divide-y divide-border/40">
				{contests.slice(0, 5).map((contest) => (
					<div class="flex items-center gap-4 px-5 py-3.5 hover:bg-muted/30 transition-colors">
						<div class="flex-1 min-w-0">
							<p class="text-[13px] font-medium text-foreground truncate">
								{contest.title}
							</p>
							<p class="mt-0.5 font-mono text-[11px] text-muted-foreground truncate">
								{new Date(contest.startTime * 1000).toLocaleDateString()} · Rank
								#{contest.rank.toLocaleString()} · {contest.problemsSolved}{" "}
								solved
							</p>
						</div>
						<div class="flex items-center gap-3 shrink-0">
							<span
								class:list={[
									"font-mono text-xs font-semibold tabular-nums px-2 py-0.5 rounded-full border",
									contest.ratingChange >= 0
										? "text-emerald-500 bg-emerald-500/10 border-emerald-500/20"
										: "text-rose-500 bg-rose-500/10 border-rose-500/20",
								]}
							>
								{contest.ratingChange > 0 ? "+" : ""}
								{contest.ratingChange}
							</span>
							<div class="text-right">
								<p class="font-mono text-sm font-bold text-foreground tabular-nums leading-tight">
									{contest.score}
								</p>
								<p class="text-[10px] text-muted-foreground leading-tight">
									pts
								</p>
							</div>
						</div>
					</div>
				))}
			</div>
		) : (
			<div class="p-4">
				<EmptyState
					size="sm"
					command="leetcode contests --list"
					title="no contest data available"
				/>
			</div>
		)
	}
</section>
