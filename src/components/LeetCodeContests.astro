---
import { leetCodeData } from "../data/leetcode";
import { Trophy, Calendar, Users, TrendingUp } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const { contests } = leetCodeData;
---

<Card className="bg-card/50 backdrop-blur-xl border-border/50 shadow-lg">
	<CardHeader>
		<CardTitle className="text-lg font-semibold text-foreground">
			Recent Contest Performance
		</CardTitle>
	</CardHeader>
	<CardContent>
		{
			contests.length > 0 ? (
				<div class="space-y-4">
					{contests.slice(0, 5).map((contest) => (
						<div class="p-4 bg-card/60 backdrop-blur rounded-xl border border-border/50 hover:bg-card/80 transition-all duration-300">
							<div class="flex-1">
								<div class="flex items-center space-x-3 mb-3">
									<div class="p-1.5 bg-primary/10 rounded-lg">
										<Trophy className="w-4 h-4 text-primary" />
									</div>
									<h4 class="font-medium text-foreground">{contest.title}</h4>
								</div>
								<div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
									<div class="flex items-center space-x-2">
										<div class="p-1 bg-secondary rounded">
											<Calendar className="w-3 h-3 text-muted-foreground" />
										</div>
										<span class="text-muted-foreground">
											{new Date(contest.startTime * 1000).toLocaleDateString()}
										</span>
									</div>
									<div class="flex items-center space-x-2">
										<div class="p-1 bg-secondary rounded">
											<Users className="w-3 h-3 text-muted-foreground" />
										</div>
										<span class="text-muted-foreground">
											Rank #{contest.rank.toLocaleString()}
										</span>
									</div>
									<div class="flex items-center space-x-2">
										<div class="p-1 bg-secondary rounded">
											<TrendingUp className="w-3 h-3 text-muted-foreground" />
										</div>
										<span class="text-muted-foreground">
											{contest.ratingChange > 0 ? "+" : ""}
											{contest.ratingChange}
										</span>
									</div>
									<div class="text-muted-foreground">
										{contest.problemsSolved}/{contest.participants} solved
									</div>
								</div>
							</div>
							<div class="text-right mt-3">
								<div class="text-xl font-bold text-foreground">
									{contest.score}
								</div>
								<div class="text-sm text-muted-foreground">points</div>
							</div>
						</div>
					))}
				</div>
			) : (
				<div class="text-center py-8">
					<div class="p-3 bg-muted dark:bg-secondary rounded-xl w-fit mx-auto mb-4">
						<Trophy className="w-8 h-8 text-muted-foreground mx-auto" />
					</div>
					<p class="text-muted-foreground dark:text-muted-foreground">
						No contest data available
					</p>
				</div>
			)
		}
	</CardContent>
</Card>
